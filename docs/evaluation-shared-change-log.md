# 跨G共享变更记录

只记录会改变多个G组理解或显示的底层变化；单条规则内部修正仍由提交和状态表追踪。

| 日期 | Commit | 共享变化 | 受影响范围 |
|---|---|---|---|
| 2026-08-05 | `74bc48c` | `functionTags`成为用途分类依据；Item/Shelf接入方向、最大开启深度和最小使用空间；轻型座椅与普通摆放家具分离。 | G1对象表达、G2洁具/设备识别、G3通行与操作区 |
| 2026-08-05 | `3b24ec9` | 家具图层纳入Shelf；厨卫图层纳入电器；操作区进入统一画布图层。 | G3家具、厨卫、电器及UI |
| 2026-08-06 | `e0177c6` | 床头柜接入显式抽屉区；办公椅不再套用扶手椅前方区；水槽、淋浴房和厨电优先使用显式字段。 | G3-011、G3-020、G3-026、G3-029、G3-036及UI |
| 2026-08-06 | `baf2dc4` | 开启范围与开启后人员使用区分离；开放Shelf前后取物区不再显示为门扇开启。 | 所有显式可开启Item/Shelf及画布图例 |
| 2026-08-06 | （本次修正） | 补齐洗碗机和独立烤箱的显式用途语义；普通灶具不推定为烤箱；Handoff Schema与当前Item/Shelf投影对齐。 | G2/G3设备语义、G3-029、G3-032及Evaluator Handoff |
| 2026-08-06 | （本次修正） | 统一报告卡片恢复画布反向定位：点击评价高亮后滚动到对应卡片、标示当前卡片，并保留多对象目标序号。 | G1至G4统一报告UI与画布评价高亮 |
| 2026-08-06 | （本次修正） | 显式开启方向、开启深度和最小使用空间保持为权威源数据；完整操作包络独立对照Slab建筑范围，不受用途或Room/Zone匹配影响。 | G3操作区、厨房设备及画布高亮 |
| 2026-08-07 | （本次修正） | 显式开启范围改按刚体扫掠判碰撞；开启后人员使用区单独按可用比例和可达性判断。衣柜不再由储物柜规则重复报警。 | G3-010、011、012、018、029、039、042及操作区画布/报告 |
| 2026-08-07 | （本次修正） | 共享操作评估显式暴露最小使用空间的面积、阻挡、房间内比例、可达性与可用状态；S1-FUR 复用这些证据和 `functionTags` 语义，不另建碰撞结论。 | G3共享操作证据、S1-FUR、S1报告与画布 |
| 2026-08-10 | （本次收口） | 产品规则执行范围改为飞书已确认的6条G1和7条G3；家具/柜体/设备开启、最小人员使用空间及3D实体碰撞退出平面G评价。G1-023仅检查对象主体建筑边界，G3-013仅检查基本路径可接近窗户。 | G1/G3执行入口、报告汇总、操作区语义与UI |
| 2026-08-11 | （本次修正） | 导航层正式提供由Door Portal实际可达的自由格；G3-013直接复用该结果，不再按显示坐标重建栅格连通性。 | G3-013、导航报告证据与画布调试数据 |
| 2026-08-11 | （本次修正） | 通行画布改为产品化精简展示：普通定位不再自动显示路径、节点和栅格；辅助开关只显示Door Portal真实可达范围，并使用房间局部栅格原点合并绘制。 | G3通行证据、G3-013及统一画布UI |
| 2026-08-11 | （本次修正） | V0.1单人基本通行宽度由600毫米调整为400毫米，等效人员半径由300毫米调整为200毫米；明确该二维人体中心模型不能代替伸手操作窗户的三维触达判断。 | G3-003、G3-013、共享导航与画布说明 |
| 2026-08-11 | （本次收口） | G3-013从产品白名单、规则实现、页面文案和回归结果中移除；飞书规则状态同步为已取消并清空关联参数。底层Window、Room和导航证据继续供G2及其他能力复用。 | G3执行范围、报告汇总、UI与飞书规则母表 |
| 2026-08-11 | （本次修正） | G4作为客户需求初筛在产品流程中前置；需求统一视为必须满足，不再传递或显示客户优先级。G4未满足或需人工判断时暂停自动流程，但允许用户主动继续G1–G3。 | Requirement Handoff、G4执行编排、G1–G4统一报告与UI |

后续共享修改追加一行，不重写历史。若旧审计文档与当前代码冲突，以统一语义底座、状态表、代码和测试为准。

# 2026-08-12 · S1 动线 POC 开发调试图层

- 新增仅开发环境可见的 `Current / Yuka / Visibility Graph` 路径对照切换；正式报告、G1—G4 状态、HPE 生产 `pathPoints` 和中性 S1 正式定位均未改变。
- POC 候选使用统一 0.20m 实验 agent radius、RoomRegion/hole 与家具实体 footprint，并自动验证 clearance、Portal 和确定性；开启/使用空间与三维物理区域不参与。
- 影响范围：共享 `EvaluationHighlight` 仅增加可选开发数据；不改变任何 G 规则结论或用户状态语义。

# 2026-08-12 · S1-HPE 生产 Visibility Graph

- HPE 正式路径改为 Room/Portal 全局 deterministic Dijkstra 与房内 Polygon Visibility Graph，旧自由网格仅作开发 shadow。
- 生产 clearance 使用 0.20 m 技术半径和 round offset，并由原始 Room/障碍几何独立复核；该参数不改变 G1—G4 法规或使用性阈值。
- 有可靠 footprint 的落地家具（含轻型座椅）保守阻挡 HPE；开启、最小使用与三维操作范围不参与。PCI 只消费新正式路径，PCP 算法不变。

# 2026-08-13 · S1-HPE 行为端点收口

- Door 新增可选 `isPrimaryEntrance` 并显式贯通 Parser、Evaluation Handoff 与 DoorPortal；不以名称猜主入口。
- HPE 归家路线从住宅侧 DoorPortal landing 到首次进入正式 Kitchen Zone 的合法边界，多床逐床且只采样可靠长边；同一主入口/车库门的重复归家行为合并。PCI 自动消费更新后的 measured 路线，PCP 与 G1—G4 结论不变。

# 2026-08-13 · S1-HPE 床边世界方向修正

- 床边不再按世界 footprint 的长短边推断；改为复用 Item `resolvedRotationRadians` 的 local-to-world 变换，以局部 Z 作为床头→床尾、局部 X 两侧作为正常下床侧。
- 近方形床不再因为尺寸比而 `unable_to_determine`；仅实际缺失中心点、世界方向、尺寸或 footprint 时无法测量。影响 HPE 和其 PCI 输入，PCP 与 G1—G4 结论不变。

# 2026-08-13 · S1-HPE v0.1 Demo 局部评分

- HPE 增加独立评分层：以合法 SF 编码、可靠 Zone–RoomRegion 有效几何按楼层并集计算 Demo 有效住宅室内面积；SF24、SF30、SF32、SF50+ 与未编码 Zone 不计入。
- 路线评分消费既有正式行为端点和 Polygon Visibility Graph 测量，不改变寻路、PCI 输入、PCP 或 G1—G4；报告只展示 HPE 局部分数，不生成 S1 总分。

# 2026-08-13 · S1 正式面板收口

- 正式用户 S1 面板当前只展示已冻结的 HPE 动线效率局部分数、两个生活场景和路线核心结果；FR、PCP、PCI、SFS、FUR 保留底层测量但不进入正式用户面板。
- 路线卡只显示实际距离、绕行程度、路线得分和画布定位；RoomRegion、Portal、对象 ID、置信度及调试数据不再占用正式报告首层。

# 2026-08-13 · S1 正式执行入口收口

- S1 正式按钮只执行 HPE 高频路径测量与 v0.1 Demo 局部评分；FR、PCP、PCI、SFS、FUR 不再由正式入口计算，底层模块与历史测试保留。
- S1 评分复用方案检查的旋转加载图标、阶段进度条、按钮禁用和耗时反馈；不改变 G1—G4、HPE 寻路或评分结果。

# 2026-08-13 · SDI 入户支持空间与空间组织轴

- 正式 SDI 目录补充 SF34 食品储藏间、SF35 泥房、SF36 儿童活动区；Parser、Handoff、G4 与 S1 继续通过统一目录和语义入口读取。
- S1-SO 复用 RoomConnectivityGraph、DoorPortal、`isPrimaryEntrance` 与既有 FR 分数；只新增空间组织 Demo 评分，不改变 G1—G4、HPE、PCP、PCI 或 S1 总分。
- SF02 的权威语义同时映射为 `kitchen`、`closed_kitchen`、`chinese_kitchen`：SDI 当前以“封闭厨房”表达中厨类型，G4 不再依赖 Zone 名称识别中厨需求。

# 2026-08-13 · S1 动静分区语义分类

- 新增集中、穷尽的室内 SDI `active / quiet / neutral` 分类，仅供 S1-DZ 读取；Zone 名称、HPE 距离和 G1—G4 结论均不参与该分类。
- S1-DZ 复用 RoomConnectivityGraph 与真实 DoorPortal，报告可中性定位对应 Zone、RoomRegion 与门；不改变 G1—G4、PCP、HPE、PCI 或空间组织结果。

# 2026-08-14 · S1 空间利用评分

- S1-LY 复用 HPE 已验证的有效住宅面积口径，以及 SFS 已有的二维 Zone–RoomRegion 形状定义；只新增 S1 局部评分，不改变共享面积、G1—G4 或 SFS 原始测量结论。

# 2026-08-14 · S1 收纳配置评分

- S1-SN 只复用现有 SDI `spaceFunctionCode`、权威 Item/Shelf `functionTags`、`dimensionsMeters` 与 RoomConnectivityGraph/DoorPortal 证据；不新增共享语义或几何字段，不改变 G1—G4、SO-03、HPE 或其他 S1 轴结论。

# 2026-08-14 · S1 v0.1 aggregate 与正式报告

- S1 总分、覆盖状态与雷达只读取同一五轴 aggregate；G1—G4 继续作为 allow-list 准入，`not_applicable` 重新归一化，`unable_to_determine` 不按零分且阻止不完整总分。
- 复用既有 HPE、DZ、LY 与收纳的画布定位证据，不新增第二套 S1 对象识别、几何或状态语义。

# 2026-08-17 · 设计师界面信息减法

- 左栏默认只保留图层入口、方案检查和按需出现的对象检查信息；文件统计、解析诊断、演示入口和路径调试均移入开发信息或仅开发环境显示。
- 综合问题卡首层不再展示对象 ID、规则 ID 或原始单位；保留可读定位、原因、建议和画布定位，完整 RuleResult、测量、缺失数据及置信度继续收在“查看详情”。
- 本次仅调整展示层级和文案；不改变 G1—G4、S1 的评价结论、排序、统计、图层开关状态或画布定位逻辑。

# 2026-08-17 · S1 设计师面板信息减法

- S1 默认以五轴雷达为核心展示设计表现总分，分数置于雷达中心，五个轴的中文名称标在对应雷达轴端；五个正式轴作为可展开入口呈现。规则编号、版本、权重、适用分母、技术状态与运行耗时不再占用首层。
- 各轴改为按需展开，只保留设计师可理解的评价说明、关键结果和真实画布定位；SF 编码、几何指标、对象 ID、诊断原文、逐对象统计与算法过程不在正式首层展示。
- 删除“尚未纳入 S1 总分”的过期文案；本次仅改变 S1 呈现层级和文案，不改变五轴、总分、准入、计算、状态或定位逻辑。
- 移除左栏可见的 S1 路径开发调试与 POC 寻路方案选择；这些只服务研发排错，不属于设计师操作，且不改变正式路径或画布定位。

# 2026-08-17 · 设计师工作台视觉整理

- 左栏、画布、综合报告和 S1 五轴雷达统一为浅色设计工具视觉层；评价状态仍只由原有状态色表达，图层、画布定位、规则结果、统计和评分计算均未改变。
- 五轴雷达继续读取同一份 S1 aggregate，仅改善容器、网格、结果面与轴详情的可读性；不引入新的评分字段或展示结论。
- 画布容器与 SVG 背景使用同一颜色，避免 SVG 视图比例不同造成中间出现一块颜色不同的区域；不改变视图范围、缩放、拖拽或绘制内容。
- 第二轮仅继续压缩非工作内容的视觉权重：折叠时的图层入口更轻，画布和工具栏成为主工作面；不改变图层默认值、画布交互或评价/S1 展示内容。
- 第三轮继续整理综合问题卡的排版、状态提示和画布定位按钮层级；卡片字段、状态配色、排序、筛选、详情内容和画布联动均不改变。
- “在图中查看”仍保留同一评价高亮、对象选择和对应楼层切换，但改为整层适配视图，不再将画布缩放到单个问题对象；画布内直接点击高亮时原有视图保持不动。
- 左栏由 332px 收窄至 312px，S1 雷达同步收至可读的 232px；不改变雷达轴、分数、准入或计算。

# 2026-08-17 · 评价组名称回归

- 综合报告筛选入口统一使用既定的 `G1 基础成图`、`G2 技术安全`、`G3 功能使用`、`G4 客户需求` 名称；只修正展示文案，不改变筛选、规则、统计或状态。

# 2026-08-18 · 室内设计师暖色工作台

- 工作台改为左侧现有图层与对象信息、中间现有平面画布、右侧现有方案检查与住宅设计表现的三栏视觉布局；未新增、删除或替换任何功能入口。
- 全局视觉调整为奶油白、暖灰与橙色品牌强调，评价问题、待核验、通过、测量和关联对象仍保留原有独立状态色与画布语义。
- 本次只移动既有展示组件并修改 CSS；评价执行、筛选、统计、S1 评分、画布操作、定位联动、字段和文案均未改变。

# 2026-08-18 · 室内设计师工作台 v0.2

- 左侧现有“显示图层”默认展开，并将原有图层开关与评价辅助图层整理为紧凑控制列表；没有新增图层、工具或操作。
- 右侧方案检查使用同一份 `RuleResult` 状态计数增加环形摘要，并将原有状态、标题、定位、实际结果、处理建议和详情重排为设计师卡片层级；没有新增评价分类或推导结论。
- 问题、待核验、通过和不适用继续直接映射既有正式状态；G4继续使用“未满足客户需求 / 需要人工检查 / 已满足”的专用文案，评价计算、筛选、G4前置流程和画布定位均未改变。

# 2026-08-18 · 室内设计师工作台 v0.3

- 现有方案检查和住宅设计表现从右侧展示栏回到左侧设计师信息栏；右侧栏移除，画布获得全部剩余工作空间。报告、状态计数、G4专用文案、筛选、评分和画布定位仍读取同一份既有数据与逻辑。
- 现有“显示图层”及全部既有辅助图层开关移至全局单位左侧的可展开浮窗；未增加图层、辅助信息或图层语义。
- 用户明确授权新增仅作用于布局的左栏宽度拖拽分隔条（320–560px，亦支持键盘微调）；它不写入、不推导也不影响任何评价数据或画布内容。
- 画布工作区关闭容器滚动并让多画布在可见工作区内排列；SVG 基底与画布统一为暖白色，消除视图中的白色矩形。视图范围、缩放、拖拽、测量及绘制逻辑均未改变。

# 2026-09-03 · 画布对象隐藏 Beta

- 新增仅当前浏览器会话有效的画布对象隐藏历史：Item、Shelf、Slab、Zone、Wall、Door、Window 与 Stair 可在选中后从基础平面图层隐藏，并可恢复全部。
- 隐藏状态不写回 Pascal JSON、不修改节点、坐标、墙体几何或任何 Evaluation Handoff；G1–G4、S1 的计算、证据和结论不受影响。`⌘/Ctrl+Z`、`⌘/Ctrl+Y` 与 `⌘/Ctrl+Shift+Z` 仅撤销/重做此显示历史，编辑型表单控件仍保留其原生撤销行为。
- 画布缩放改用可取消的原生 `wheel` 监听和 `touch-action: none`；所有垂直滚轮事件（包括高精度鼠标的首帧）与触控板捏合均按光标锚点缩放，画布平移继续使用既有拖拽。此输入层变更不影响画布世界坐标、评价几何或计算结果。
- 触控板捏合缩放灵敏度单独提高：Chromium `ctrlKey` 捏合使用鼠标滚轮四倍的响应，Safari 手势采用 3.2 倍缩放曲线；鼠标滚轮手感保持不变。

# 2026-09-03 · Pascal JSON 只读 3D Viewer

- 新增与二维画布并列的只读 3D 展示入口，使用 Parser 数据的独立克隆载入 Pascal Viewer；保留源对象 ID 和变换，不写回导入 JSON、`Parsed`、Evaluation Handoff 或共享几何。
- 3D 相机、视图预设、图层、楼层模式、墙体模式、漫游及隐藏同步均只影响当前展示会话。G1–G4、S1 的数据、证据、计算、状态、统计和结论完全不变；评价问题定位仍强制返回 2D。
- 源文件没有 `roof` / `roof-segment` 时明确显示缺失提示，禁止虚构屋顶。为保证大型场景稳定，3D 不加载 GLB；家具统一以源尺寸和缩放得出的占位展示，不产生评价推论。
- 3D 楼板展示改为读取 Pascal `slab.elevation` 的向上挤出厚度，并只将 polygon 内的普通落地展示家具抬到该楼板顶面；这是只读视觉对齐，不改变二维坐标、共享几何、Evaluation Handoff 或任何评价结论。

# 2026-09-05 · 管线 Overlay 1.2 与运行时浅槽

- 管线 Demo 的独立 Overlay 升级为 1.2：红、蓝、白线管的所有非共线中心线转角统一规划为 200 mm 相切大弯；该规则不读取宿主 ID，也不修改 Pascal JSON、Parser、Evaluation Handoff 或任何 G1–G4/S1 结论。
- Tab 穿透保存入口宿主、入口、出口与冻结的入射方向；旧单点穿孔仍可读取，并明确标记为派生兼容数据。普通宿主面绘制不会因穿透产生无宿主坐标，世界 XYZ 仍是唯一的悬空绘制入口。
- 旧 `wall-chase` 自动迁移为通用 `surface-chase`；电气贴面暗敷的墙体和楼板直线/圆弧槽只切割运行时建筑克隆，天花与消防不切槽。切割失败保留 Overlay 并使用替代槽线，不影响二维几何或评价数据。
- 3D 相机仅以世界地面作为轻微边界，墙、天花、家具、管线和隐藏节点均不注册为碰撞体；该展示交互不改变场景几何、Parser 或评价数据。

# 2026-09-05 · 管线 Overlay 2.0 设备与来源拓扑

- 管线侧车文件新增强/弱电箱、消防入户点、插座、开关、灯具、网络面板、向上喷淋头、Circuit 与通用物理端口；旧 Overlay 系统名迁移为插座/照明/网络/喷淋并保留未接源诊断。
- 设备、来源约束、白色网络无分支规则和喷淋头三通短支管只属于独立管线 Demo，不写回 Pascal JSON，不改变 Parser、共享几何、Evaluation Handoff 或任何 G1–G4/S1 证据与结论。

# 2026-09-05 · 管线点位放置与施工面板精简

- 管段内插入设备不再套用设备手动放置的默认宿主限制；点位单击、双击与 Enter 复用同一提交路径。电箱输出端口在箱体表面自动错位并按新线路动态增加，避免多回路共用同一几何起点。
- 管线 Viewer 只保留施工态；200 mm 大弯和 4000 mm 定尺仍作为固定 Overlay 参数参与规划，但不再暴露手动编辑入口。以上变化仅作用于管线 Demo，不改变 Pascal JSON、Parser 或评价语义。

# 2026-09-05 · 管线实时预览性能整理

- 高频指针命中合并为每个动画帧至多一次展示更新；点击、双击、Enter 和 Tab 仍读取最新原始命中点，预览与最终落点精度及管线路由语义不变。
- 2D 永久管网与瞬态草稿拆分渲染，3D 改为按需帧循环；建筑节点、楼层、洞口和楼板关系建立会话索引，空槽孔集合保持稳定引用，避免仅移动鼠标时重建建筑浅槽 CSG。
- 管线碰撞空间索引按不可变 Overlay 引用缓存，源设备通过显式对象 ID 忽略；CSG 中间几何及被替换几何会及时释放。上述优化不修改 Pascal JSON、Overlay 文件格式、Parser、共享几何定义或任何评价证据与结论。

# 2026-09-05 · 槽孔延迟生成与点位性能整理

- 管线提交仍立即保存完整浅槽与穿孔语义，但运行时建筑布尔切割改由“生成/更新槽孔”显式触发；按钮只表达是否存在未应用变化，不展示数量。按钮触发前不渲染槽体，触发后仅在真实切割失败的宿主显示替代槽线；Overlay、导出、撤销和施工关系不丢失。
- 点位管段预览按动画帧合并；不可变 Overlay 在本地历史与 2D/3D 同步间保留结构共享，设备诊断和槽线可见性查找改用索引，避免放置轻量设备时复制整网或重算全部 CSG。
- 3D 悬浮编辑面板继续支持滚动，但隐藏原生滚动条。以上均为管线 Demo 的运行时与展示优化，不修改 Pascal JSON、Parser、共享几何定义、Evaluation Handoff 或任何评价结论。

# 2026-09-07 · 管线 Overlay 2.1 悬空设备与地面过桥弯

- 独立管线 Overlay 升级为 2.1：设备可显式挂载到建筑表面或管段，86底盒端口迁移为盒体边缘的四面双孔模型；开放管端、设备端口、消防弯头和地面电气过桥弯均只属于管线 Demo 的 Overlay 与运行时展示。
- 红、蓝、白线路的同楼板非平行交叉可将新画线路提升为双45度过桥弯；消防交叉、白线分支、网络面板续线均继续禁止。槽孔切割仍由显式更新按钮触发。
- 本次不读取或写入 Pascal JSON、Parser、共享建筑几何、Evaluation Handoff 或 G1–G4/S1 输入、证据和结论。

# 2026-09-07 · 管线开放端续画与灯具端口修复

- 开放管端续画在起点只放行与原管端的同一点物理接触，不再被碰撞器误报为实体重叠；其余重叠与交叉仍会阻止提交。
- 插座、开关的已占 86 底盒孔可被显式点击，既有管线原子换到同侧另一孔；悬空灯具始终水平朝下，接管端口限定在圆盘水平面。
- 以上仅修改独立管线 Overlay、交互预览与运行时展示，不读取或写入 Pascal JSON、Parser、共享建筑几何、Evaluation Handoff 或任何评价规则及结论。

# 2026-09-07 · 86 底盒统一孔位与跨系统过桥弯

- 管段中部插入的插座/开关会侧移盒体以保持既有管段中心线不变；插座、开关与红蓝分支 86 盒统一为四面双孔模型，点击已占孔时既有线路原子换到同侧另一孔而不拉弯旧管。旧三孔分支盒在导入时迁移为八孔模型。
- 同一楼板顶面的红、蓝、白线路任意不同系统组合均可使用新线路抬升的过桥弯；消防及不合法交叉仍会阻止提交。
- 本次仅修改独立管线 Overlay、交互和运行时展示，不读取或写入 Pascal JSON、Parser、共享建筑几何、Evaluation Handoff 或任何评价规则及结论。

# 2026-09-07 · 3D 管线编辑面板精简

- 3D 左侧悬浮面板改为任务驱动布局：工具固定置顶，仅显示当前工具所需参数；选择属性合并为单一区域，施工槽孔、管线图层、诊断与快捷键收进低频折叠区，历史和文件操作固定置底。
- 面板继续隐藏滚动条并保留折叠能力；本次只调整独立管线 Demo 的界面组织，不修改绘制、路由、Overlay、Pascal JSON、Parser、共享建筑几何或任何评价结论。

# 2026-09-07 · Enter 以最后确认点完成管线

- 按 Enter 完成管线时只提交已经由左键确认的草稿点，忽略仍随鼠标移动的预览点；不足两个确认点时不生成。双击仍以双击位置完成，面板“完成路径”仍以当前预览位置完成。
- 本次只区分管线草稿的确认点与预览点，不修改路由几何、Overlay 格式、Pascal JSON、Parser、共享建筑几何或任何评价结论。

# 2026-09-07 · 3D 建筑低对比度边缘线

- 3D 建筑展示为墙、楼板、天花、门窗、家具、楼梯与屋顶增加低不透明度的几何边缘线，辅助辨认体块与洞口，但不形成强烈线框效果。
- 边缘线不参与射线拾取、宿主判断、选择或管线路由；这是独立管线 Demo 的纯展示变化，不修改 Pascal JSON、Parser、共享几何、Overlay 或任何评价结论。

## 2026-09-07 — 2D 施工图画布与自动标注 v1

- 2D 显示从主界面提取至 `src/plan/`：施工图黑灰底图、灰度家具、设备 SVG 符号、独立 2D 系统色和固定屏幕线宽；UI 配色、3D 几何、模型与 Overlay schema 保持原状。
- 新增默认开启的点位标注图层：显示设备类型或现有 `device.name` 与离地高度，不显示生成编号。双击设备说明可写回现有 Overlay 名称字段；高度、数量和排列关系仍由模型派生。2D 不生成管径、管道上行/下行、方向或墙端定位文字。
- 高度严格对齐 `PascalScenePreview` 的楼层字段值 × 3.2 m 展示基准（含负数或跳号），不使用排序数组下标；施工前仍需按现场完成面复核。
- 楼层归属复用宿主父链，并由真实管件引用传播到无宿主管段；管段挂载设备仅继承唯一楼层。多楼层边界一次性收集，不依赖管件顺序；未知归属列入提示，不在全部楼层重复绘制。
- 外围尺寸继续复用既有测量与来源；房间文字、尺寸与测量数字、点位文字及点位图块均使用模型空间尺寸，随图纸缩放保持相对比例。新增全局 50%–200% 点位标注比例，只调整点位图块与点位文字，不改变几何、外围尺寸或测量值。
- 点位标注复用外围尺寸的建筑外轮廓识别，按最近外围边分组并置于总体尺寸链之外；同侧按点位顺序使用两排、以正交折线为主的引线。2D 浮窗移除开发统计与解析诊断。
- 点位引线穿越外围尺寸带时会避开该侧已有尺寸文字的占位区，再在建筑外侧连接说明块，减少引线压住尺寸数字及无意义的超长横线。设备已有可靠建筑宿主定位时，即使管段拆分后遗留的 `mount.segmentId` 已失效，2D 仍显示该点位；该兼容只修正显示可见性，不改写 Overlay。
- 左侧点位说明块的竖向主线移至文字右侧，右侧说明块移至文字左侧，上下侧仍位于文字左侧；左右关系按画布旋转后的最终屏幕方向判断。说明块以所属建筑组件的最外轮廓为基准，按其朝外方向的完整宽度或高度越过总体尺寸链及尺寸界线端部后才起竖线，引线终止于该竖线；该变化仅调整 2D 派生排版。
- 同一建筑组件同一侧的点位说明块优先使用统一的主竖线位置；仅当相邻文字块小于半行高度间距时使用外移副列，后续不冲突的说明块自动回到主列。各说明块的短竖线保持分离，不连接成长线，左右两侧采用镜像规则。
- 同侧发生局部碰撞时从该侧末端反向分配竖线列，优先让后续连续且互不冲突的说明块共用主列；例如由上至下仅 1、2 冲突时，结果为 1 使用副列而 2、3、4 使用主列。
- 点位引线按画布旋转反算说明块边缘的真实连接点，直接终止在说明块左／右短竖线上，不再经过文字中心形成数字下方的无意义短线；说明块背景增加轻微外扩遮罩，避免外围尺寸界线紧贴文字边缘露头。
- 2D 点位楼层归属补充读取 Overlay 2.1 的安装参考面挂载，使灯具、喷头等参考面设备显示在其明确楼层。墙面插座、开关、网络面板、强弱电箱及消防入户点按保存的墙面法向或设备 `frame.front` 旋转，顶面点位继续保持屏幕正向；不改写设备位置、挂载或 3D 朝向。
- 新增独立的“点位定位尺寸”图层：墙面点位以设备中心为终点，优先引用同墙最近门窗洞口边，否则引用最近墙端；灯具与喷头等顶面点位从中心向两面互相垂直、投影实际落在有限直墙段内的模型墙面生成两向尺寸。曲墙、无可靠宿主或无可靠有限墙段时不猜测尺寸。尺寸均为模型派生值，不写回 Overlay，也不改变 G1/G2/G3 评价语义。
- 同墙同类型点位的定位尺寸改为连续尺寸链：首段从洞口边或墙端到首个设备中心，后续逐段标注相邻设备中心距。尺寸线使用墙轴投影点保证与直墙平行，尺寸界线由设备实际中心垂直引出；不同类型使用相邻尺寸列，避免把不相关点位串成同一链。
- 同墙相邻中心距离不超过 150 mm 的普通点位自动形成安装组；同类型同高度合并数量，水平组合派生左/中/右，重合投影的不同高度组合派生上/中/下。说明块保留主干与逐行分隔线；来源设备不与普通终端合并。
- 保留测量计算、吸附、正交和公英制切换；3D 草稿仍独立渲染且不生成永久标注。默认样例及空 Overlay 未修改。
- 影响范围：仅施工 Demo 的二维显示层；G1/G2/G3 的输入、测量、状态和结论均不变。本次未改 Parser、共享墙体几何或规则阈值。
- 验证：新增纯数据/排版及 jsdom 组件测试，覆盖地下层/跳层、挂载设备、隐藏系统、外围分组、两排布局、标注比例、模型编辑/删除、无管道文字与临时草稿隔离。浏览器视觉验收按 README 由用户执行。

## 2026-09-07 — 设备点位精确定位与局部手动重连

- 3D Overlay 编辑新增墙上设备边缘净距、安装参考平面设备标高、预览后提交和 Ctrl/Command 多选标高修改；缺失天花或梁时记录显式的每层安装参考平面，不伪造 Pascal 建筑宿主。
- 移动已连接设备会原子删除直接相邻管段及其专属施工依赖，修复保留侧端口引用并留下真实开放端；界面短暂显示被移除管段，撤销/重做仍以完整 Overlay 快照为单位。
- 新增受世界轴、宿主面正交约束的对象捕捉解析层，限定为兼容设备端口、开放端、合法分支和宿主边角；不加入数值管长或自动路线。
- 本次只修改独立管线 Overlay 和 3D 编辑体验；不修改 Pascal JSON、Parser、共享建筑几何、Evaluation Handoff 或 G1-G4/S1 规则与结论。

## 2026-09-07 — 3D 点位多选与射线尺寸参照修正

- Ctrl/Command 加选的全部设备点位统一显示选中高亮；批量修改离地高度不再因当前高亮设备数值相同而跳过其他已选设备。
- 安装参考平面设备的平面尺寸只采用沿尺寸射线实际落在有限墙段上的墙面，并计入墙厚，不再选择虽近但尺寸延长线无法碰到的墙段。
- 画管捕捉或受约束辅助对齐时，在 3D 草稿端显示绿色标记及设备端口、开放管端或分支目标标签。
- 设备端口的辅助对齐与对象捕捉严格分开：正交或 XYZ 锁轴解析为 `alignment` 时，点击只确认当前预览的辅助点并继续逐点绘制；只有解析为 `snap`、即受约束路线真正到达物理端口时，点击才连接设备并结束本次画管。悬空路线缺少宿主基准时仍使用最接近目标的世界轴生成辅助对齐点，不自动补画到端口。
- 端口终点路径的碰撞预检与最终连接都排除目标设备自身，避免把合法进入目标端口的末段误报为实体重叠；其他设备、管段与管件的碰撞检查不变。
- 设备作为路线终点时保留鼠标实际点击位置所对应的物理端口，并将该端口 ID 贯穿至提交；多孔 86 底盒不再回退到第一个可用孔。过桥弯可拆分已连接普通弯头的地面交叉管段，并同步更新相邻弯头端口引用，避免辅助点使桥弯规划被跳过。
- 暂时移除 3D 面板中的“施工与槽孔”控制区；Overlay 中既有槽孔数据与渲染兼容仍保留。本次不修改 Pascal JSON、Parser、共享评价几何或规则结论。

## 2026-09-08 — 2D 同图块多高度自动分型

- 灯具、喷淋及其他进入安装高度表的点位，在同一专业内出现同图块、同安装方式但不同模型高度时，按高度从高到低自动派生 A/B/C 分型；图面点位旁与安装高度表使用相同分型。同类点位仅有一个高度时不增加字母，墙面点位继续直接显示原有标高引线。
- 分型仅由当前可见施工图的只读点位数据派生，不写回 Overlay；本次不修改 Pascal JSON、Parser、共享建筑几何、Evaluation Handoff 或 G1/G2/G3 评价语义。

## 2026-09-08 — 2D 分专业施工图层与安装高度表

- 2D 浮动面板将原有管线、点位和标注的底层开关收敛为插座、灯具、弱电、消防四类施工图；各类施工图同步控制对应管线、设备图块、定位尺寸、文字标注和图例，“全部施工图”统一联动四类图层。建筑图层、测量、3D 编辑与全局点位标注比例保持原有行为。
- 墙面设备继续在建筑外围显示设备类型与模型标高引线；安装参考面、天花、楼板及悬空设备不再生成外围标高引线，其类型、安装方式、模型高度和数量按当前可见专业汇总到图纸下方的“点位图例及安装高度表”。顶面点位原有两向定位尺寸继续保留。
- 以上均为独立管线 Demo 的 2D 派生显示与图层编排变化，不修改 Pascal JSON、Overlay schema、共享建筑几何、Evaluation Handoff，亦不改变 G1、G2、G3 或其他评价规则的证据、状态与结论。

## 2026-09-08 — 同墙端口到达与底盒目标准星

- 正交或 XYZ 锁轴画管时，同一宿主表面内的路线与物理端口只按宿主平面坐标判断是否到达；墙体法线方向的微小建模或显示偏差不再把合法连接误判为辅助对齐，最终管端仍精确落在物理端口坐标。
- 活跃路线悬停设备点位时，只显示当前系统兼容且空闲的目标端口；明确点击端口时保留该端口，点击设备主体时选择离鼠标最近的端口。目标附近的大实体游标改为空心准星，所选孔高亮，并区分“辅助对齐”和“连接后结束”。
- 本次只修改独立管线 Overlay 的捕捉判定与 3D 编辑提示，不修改 Pascal JSON、Overlay schema、Parser、共享建筑几何、Evaluation Handoff 或 G1-G4/S1 的证据与结论。

## 2026-09-08 — 正交意图滞后与固定捕捉提示

- 宿主面正交绘制以“上一确认点到原始鼠标位置”的主方向作为辅助对齐意图；当前方向会保持到另一方向达到约 1.5 倍优势后才切换。设备端口只提供该方向上的对齐位置，不再反向决定 X/Z 或宿主 U/V 方向；显式世界 XYZ 锁轴仍具有最高优先级。
- 世界 XYZ 锁轴生成的无宿主预览点在进入最终显示草稿时显式保留 `worldAxis` 约束，不再被普通宿主预览规则过滤；由最后确认点到鼠标投影点的预览继续沿用既有指针射线实时更新。跟随预览端点的文字框已移除，3D 内只保留准星、孔位和捕捉环，状态文字改为画布顶部的固定窄提示条。
- 本次只修改独立管线 Overlay 的绘制交互和 3D 提示层，不修改 Pascal JSON、Overlay schema、Parser、共享建筑几何、Evaluation Handoff 或 G1-G4/S1 的证据与结论。

## 2026-09-08 — 照明控制组

- Conduit Overlay 升级至 2.2，新增独立的 `lightingControlGroups`：每组由一个开关点位控制一组灯具，同一开关的组数派生为几开，同一灯具在 MVP 中至多属于一组。2.1 及更早 Overlay 导入时补为空数组。
- 3D 编辑器支持先 Ctrl 多选未绑定灯具、再选择开关创建一路；选择开关可按创建顺序查看、原子重选或整路解绑。选择关系成员时仅显示青色虚线逻辑关系并高亮相关点位，不生成或修改管线。
- 同一路的虚线关系以“开关连接最近灯具、灯具再按空间邻近连续连接”显示；开关不会分别向每盏灯放射连线，避免把一组多灯误读为多开开关。导入 2.2 Overlay 时同时校验组 ID、设备类型、非空成员及灯具唯一归属。
- 控制关系与照明回路、线管拓扑、楼层归属相互独立；移动点位保留关系，删除开关、灯具或空组时只清理相应逻辑引用。本次不修改 Pascal JSON、Parser、共享建筑几何、Evaluation Handoff 或 G1-G4/S1 的证据与结论。

## 2026-09-09 — 白色网络管道允许悬空起画

- 白色网络线路可在没有设备、开放管端或建筑宿主的空白空间直接确认第一个点，再按既有逐点、正交及世界 XYZ 锁轴方式预览和生成；空白鼠标位置投影到穿过当前起点的视平面，避免依赖不可见的虚构宿主。
- 该例外只适用于 `network` 系统；插座、照明和喷淋线路的合法起点约束保持不变。自由生成的白色管道不虚构设备、回路或宿主引用。本次不修改 Pascal JSON、Parser、共享建筑几何、Evaluation Handoff 或 G1-G4/S1 的证据与结论。

## 2026-09-08 — 2D 灯控关系与管道独立显示

- 灯具施工图读取 Overlay 2.2 的 `lightingControlGroups`，以无文字的蓝灰虚线圆弧显示开关至最近灯具、再连续连接组内灯具的逻辑控制关系；控制线不表示实际导线或线管路径，不生成连接节点。
- 施工图层增加全局“管道”开关，只隐藏当前已开启专业的永久管段、管件、穿孔及实时管线草稿；点位图块、灯控关系、定位尺寸、墙面标高、分型角标和安装高度表继续显示。
- 2D 墙面点位标高统一为设备下边缘相对模型楼层基准的高度，与 3D“下边缘离地”字段一致；安装参考面设备继续读取其明确保存的完成地标高。以上均为 Overlay 派生显示，不修改 G1/G2/G3 评价语义。
# 2026-09-08 — 2D 开关联数表达

- 2D 灯具施工图按 `lightingControlGroups` 中同一开关对应的独立控制组数量，派生单开、双开及多开图块和默认文字；没有控制组时保留通用“开关”，不从名称或端口数量猜测联数。
- 用户编辑过的开关名称继续优先于派生的默认类型文字。该信息仅用于施工图显示，不写回 Overlay，也不改变 G1/G2/G3 的对象语义、证据、状态或结论。
